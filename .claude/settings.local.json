{
  "permissions": {
    "allow": [
      "Bash(python -m pip install pyodbc --only-binary :all:)",
      "Bash(python -c \"import pyodbc; drivers = [d for d in pyodbc.drivers\\(\\) if 'SQL Server' in d]; print\\('可用 ODBC 驱动:', drivers\\)\")",
      "WebFetch(domain:bolt.new)",
      "Bash(npm install:*)",
      "Bash(npm list:*)"
    ]
  }
}
